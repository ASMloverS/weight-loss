{
  "name": "weight-loss-plan-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Build tooling for the cycling training plan static site (data extraction only).",
  "scripts": {
    "extract": "node scripts/extract-plans.cjs",
    "test": "node scripts/test-mapping.cjs",
    "build-index": "node scripts/build-index.cjs",
    "style": "node scripts/apply-style.cjs",
    "inject-realign": "node scripts/inject-realign.cjs"
  },
  "devDependencies": {
    "cheerio": "^1.0.0"
  }
}
